# GLM Standalone IDE (独立型AI統合開発環境) 基本設計書

最終更新: 2026-09-03  
対象: GLM Router × co-vibe 基盤の完全独立GUIアプリケーション化

---

## 1. コンセプトと目標

### 1.1 概要
GLM Standalone IDE は、VSCode や Copilot に依存せず、ローカルLLM（Ollama）および各種クラウドAIプロバイダーを自律的に統合制御する **ローカルファーストのAIコーディングGUIアプリケーション** です。

### 1.2 コア設計原則
1. **リソース最優先 (Resource Efficiency)**  
   - GPU VRAM (8GB) と CPU リソースを AI モデル（7B/1.7B/0.5B）へ最大限割り当てるため、フロントエンドのメモリフットプリントを最小化（< 100MB 目標）。
2. **ゼロ・トラスト & 安全ファースト (Security & Isolation)**  
   - エージェントのツール実行（ファイル変更、コマンド実行、ネットワークアクセス）はすべて視覚的なインライン承認カードで人間が制御。
   - ローカル外通信は SQLite 監査ログへ厳格に記録。
3. **モジュール分離構造 (Loose Coupling)**  
   - バックエンド（Python エンジン: `router.py`, `co-vibe`, `glm_security`）とフロントエンド（GUI）を非同期 API / SSE で分離。

---

## 2. 技術選定 (Tech Stack)

| レイヤー | 選定技術 | 採用理由・比較 |
|---|---|---|
| **GUI アプリ枠組み** | **Tauri v2 (Rust) + Web** | Electronと比較してメモリ消費が1/5以下（約30〜50MB）。Windowsネイティブとの親和性が高い。 |
| **フロントエンド UI** | **React + Vite + TailwindCSS** | 高速なレンダリングと豊富なコンポーネントエコシステム。 |
| **コードエディタ** | **Monaco Editor (`@monaco-editor/react`)** | VSCodeのコアエディタ。構文ハイライト、Diffビュー、IntelliSenseとの完全な互換性。 |
| **統合ターミナル** | **xterm.js + node-pty / Rust PTY** | VSCodeと同等の本格的なターミナルエミュレーション。 |
| **バックエンドエンジン** | **Python 3.14 (PyInstaller / Nuitka で .exe 化)** | 既存資産 (`router.py`, `co-vibe`, `glm_security`) の完全流用。標準ライブラリ依存。 |
| **プロセス間通信 (IPC)** | **HTTP REST + SSE (Server-Sent Events) / WebSocket** | リアルタイムなトークンストリーミング、ツール実行状況の即時描画。 |

---

## 3. 全体システムアーキテクチャ

```text
┌────────────────────────────────────────────────────────────────────────┐
│                        GLM Standalone IDE (GUI)                        │
│ ┌───────────────────┐ ┌───────────────────┐ ┌────────────────────────┐ │
│ │ ファイルツリー     │ │  Monaco Editor    │ │ GLM AI Chat & Panel    │ │
│ │ (File Explorer)   │ │  & Diff Viewer    │ │ (承認カード/思考ログ)   │ │
│ └───────────────────┘ └───────────────────┘ └────────────────────────┘ │
│ ┌────────────────────────────────────────────────────────────────────┐ │
│ │ xterm.js 統合ターミナル & システムダッシュボード (GPU/VRAM/予算)     │ │
│ └────────────────────────────────────────────────────────────────────┘ │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │ IPC (HTTP / SSE / WebSocket)
┌───────────────────────────────────▼────────────────────────────────────┐
│                  GLM Process Manager (Daemon Engine)                   │
│                                                                        │
│ ┌─────────────────┐  ┌──────────────────────┐  ┌────────────────────┐ │
│ │ GLM Router      │  │ co-vibe Agent        │  │ GLM Security       │ │
│ │ (127.0.0.1:8765)│  │ Orchestrator         │  │ Watchdog & Audit   │ │
│ └────────┬────────┘  └──────────┬───────────┘  └─────────┬──────────┘ │
└──────────┼──────────────────────┼────────────────────────┼────────────┘
           │                      │                        │
┌──────────▼────────┐  ┌──────────▼───────────┐  ┌─────────▼──────────┐
│ Ollama (Local)    │  │ Anthropic / OpenAI   │  │ ~/.glm/            │
│ - qwen3:0.5b      │  │ Cloud APIs (Optional)│  │ - audit/           │
│ - qwen2.5-coder:7b│  │                      │  │ - sessions/        │
└───────────────────┘  └──────────────────────┘  └────────────────────┘
```

---

## 4. プロセス管理 & ライフサイクル設計

### 4.1 プロセスマネージャー (GLM Daemon)
Tauri アプリの起動時に、バックグラウンドの Python デーモンプロセスマネージャー (`glm_ide_daemon.py`) をサイドカーとして自動起動します。

```text
[IDE 起動]
   │
   ├── 1. ポート重複・既存プロセスチェック (8765, 8767, 8766)
   ├── 2. ~/.glm/auth_token の生成/検証
   ├── 3. router.py (127.0.0.1:8765) のサブプロセス起動
   ├── 4. glm_approval_broker.py (127.0.0.1:8767) の起動
   ├── 5. glm_security.py (--watch) 監視プロセスの常駐
   └── 6. GUI画面への接続完了シグナル送信
```

### 4.2 終了・シグナルハンドリング
- **クリーンシャットダウン**: IDE終了時、プロセスツリー全体に `taskkill /T /F` (Windows) を発行し、残留プロセスやポートロックを防止。
- **異常検知 & 再起動**: デーモンの心拍 (`router.heartbeat.json`) が途絶した場合、GUI画面上に警告バナーを表示し、ワンクリック再起動を提供。

---

## 5. UI / UX 画面レイアウト設計

### 5.1 画面レイアウト構成 (4領域ペイン)
```text
┌───┬───────────────────────────────┬───────────────────────────┐
│ A │ B. エディタ / 差分表示         │ C. AI サイドバー          │
│   │    (Monaco Editor / Diff)     │    - ストリーミングチャット │
│ エ│                               │    - ツール実行承認カード │
│ ス│                               │    - 思考ログ (Thought)   │
│ プ│                               │    - モード切替 selector  │
│ ロ│───────────────────────────────┼───────────────────────────┤
│ ｜│ D. 下部パネル                 │                           │
│ ラ│    - 統合ターミナル (xterm.js) │                           │
│ ─ │    - GPU温度/VRAM/月次予算バー │                           │
└───┴───────────────────────────────┴───────────────────────────┘
```

#### 領域 A: サイドバー・ナビゲーション
- ファイルエクスプローラー（ワークスペースツリー、作成・削除・リネーム）
- セッション履歴（過去のチャット・作業ログの閲覧と復元）
- スキルカタログ（`skills/<name>/SKILL.md` の閲覧・有効化スイッチ）
- 設定（APIキー管理、ローカルモデル選択、自動承認フラグ）

#### 領域 B: メインワークスペース (Monaco Editor)
- タブ型多重ファイルエディタ
- Git 差分ビューアー: エージェントが提案したコード変更を左右比較 (Side-by-side Diff) 形式で表示し、「変更を適用」「却下」ボタンを提供。

#### 領域 C: GLM AI 制御パネル
- **チャットタイムライン**: Markdownレンダリング、コードブロックコピー。
- **インライン承認カード (Interactive Approval Card)**:
  ```text
  ┌─────────────────────────────────────────────────────────┐
  │ ⚠️ ツール実行承認要求                                    │
  │ ツール: Write                                            │
  │ パス: src/utils/helpers.py                              │
  │ ─────────────────────────────────────────────────────── │
  │ [一度許可 (Y)]  [常に許可 (A)]  [拒否 (N)]  [常に拒否 (D)]│
  └─────────────────────────────────────────────────────────┘
  ```

#### 領域 D: ステータス & ターミナル
- **リアルタイムメトリクス**:
  - GPU 温度 (例: `48℃`), VRAM 使用量 (例: `3.2 / 8.0 GB`), クラウド消費額 (例: `$1.20 / $15.00`)。
- **xterm.js 統合ターミナル**: PowerShell / Cmd と直接対話。

---

## 6. 配布パッケージ & ビルド方式 (Distribution)

### 6.1 パッケージング構成
ユーザー環境へのインストールを簡略化するため、必要なコンポーネントをオールインワンでパッケージ化します。

```text
GLM-IDE-Setup-v1.0.exe (Tauri Bundler Installer)
├── GLM-IDE.exe (Tauri/Rust GUI バイナリ)
├── resources/
│   ├── engine/ (PyInstaller でビルドされた Python スタンドアロン環境)
│   │   ├── glm-engine.exe (router.py + co-vibe 統合実行ファイル)
│   │   └── glm-security.exe
│   └── default_config/
│       ├── registry.json
│       ├── settings.json
│       └── models/
```

### 6.2 初回セットアップ・ウィザード (Onboarding Wizard)
1. **動作環境自動チェック**:
   - NVIDIA GPU & nvidia-smi の検出
   - Ollama の常駐確認（未起動の場合は `ollama serve` の自動呼び出しを試行）
2. **モデル自動取得案内**:
   - `qwen3:0.5b` (nano-gate) および `qwen2.5-coder:7b` (メインモデル) の存在確認とワンクリック取得ボタン。

---

## 7. 実装ロードマップ (Standalone IDE)

- **Phase A: バックエンド API / SSE リアルタイム化 (1-2日)**
  - `router.py` および `co-vibe` に SSE (Server-Sent Events) ストリーミングエンドポイントを追加。
- **Phase B: Tauri GUI スケルトン & Monaco エディタ構築 (2-3日)**
  - Tauri 2.0 プロジェクト作成、Monaco Editor、xterm.js の組み込み。
- **Phase C: チャット・インライン承認カード UI 実装 (2日)**
  - チャットタイムライン、承認カード、`glm_approval_broker` との非同期連携。
- **Phase D: システムダッシュボード & パッケージング (1-2日)**
  - メトリクス表示、PyInstaller + Tauri Bundler によるワンクリックインストーラー生成。

---

以上の設計に基づき、段階的に構築を進めることが可能です。
