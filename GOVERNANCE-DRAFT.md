# GLM ガバナンス たたき台（B-2 リスク分類 / B-3 承認スキーマ）

本ドキュメントは合意事項ではなく、確認・調整のためのたたき台です。

## B-2. 操作リスク分類（たたき台）

| リスクレベル | 例 | 承認要否 | 実装状況 |
|---|---|---|---|
| 低 | ファイル読み取り、diff表示、LSP診断 | 不要 | 実装済み |
| 中 | ワークスペース内のファイル作成・編集 | 設定次第（allow/deny一致で自動判定） | 実装済み |
| 高 | ファイル削除、`git push`、外部ネットワーク通信 | 常時承認（許可リストより優先） | 実装済み（`router.py: HIGH_RISK_CATEGORIES`） |
| 最高 | 資格情報アクセス、システムコマンド実行、隔離設定変更 | 常時承認＋理由入力必須 | 実装済み（同上 + reason必須） |

### 実装済みの高リスクカテゴリ

- `file_delete`: ファイル削除・フォーマット操作
- `git_push`: リポジトリへの外部反映操作
- `external_network`: `curl`/`wget`等の外部通信コマンド
- `credential_access`: `.env`・SSH鍵・シークレットディレクトリ・認証トークンへのアクセス
- `system_command`: レジストリ削除・`sudo`・`chmod`・強制終了・シャットダウン等

これらは `settings.json` の許可リストに一致していても、常に承認（`ask`）を要求します。

### 未整理（要相談）の攻撃ベクトル

- パストラバーサル（対策済み: `WorkspaceGuard`）
- シンボリックリンク経由の脱出（未対策）
- 環境変数インジェクション（未対策）
- 依存パッケージ経由のサプライチェーン攻撃（未対策）
- プロンプトインジェクション（外部ファイル・Web内容経由）（未対策）
- Zip Slip（アーカイブ展開時のパス外書き込み）（未対策）
- 大量ファイル生成によるディスク枯渇（部分対策: プロセス数・メモリ制限のみ）
- fork bomb的な再帰的サブプロセス生成（緩和: WSL2側`ulimit -u`）

これらは次フェーズで個別に対策要否を判断します。

## B-3. 承認スキーマ（たたき台）

`glm_approval_broker.py` の `/request` エンドポイントに、以下のフィールドを追加しました。

```json
{
  "id": "req-001",
  "tool": "git_push",
  "params": {"remote": "origin", "branch": "main"},
  "target": "origin/main",
  "reason": "モデル生成コミットの反映",
  "scope": "リポジトリ全体",
  "expires_in_minutes": 15,
  "requested_by": "session-42"
}
```

| フィールド | 必須 | 現状の扱い |
|---|---|---|
| `id` / `tool` / `params` | 必須 | 従来通り必須 |
| `reason` | 必須 | 未指定は400エラーで拒否 |
| `target` / `scope` / `expires_in_minutes` / `requested_by` | 高リスク操作のみ必須 | `file_delete`/`git_push`/`external_network`/`credential_access`/`system_command`/`package_install`該当時のみ必須。それ以外は任意（未指定時はデフォルト値） |

有効期限の運用方針: 承認は既定で「1回限り」（`ApprovalBroker.respond`は一度決定すると再応答不可）。「常に許可／常に拒否」はVS Code拡張の4択UIでユーザーが明示操作した場合のみ設定可能とし、自動では設定されない。

## 未確定事項（要相談）

1. リスクレベル表の粒度は現行4段階でよいか、業務要件に応じて追加が必要か
2. 攻撃ベクトル一覧のうち、今回のスコープで対策するものと将来対応にするものの優先順位（→6項目全て実装済み、下記参照）

## Slack等の外部通知に関する禁止事項（AI倫理・外部送信共通ルール）

特定のプロジェクトに限らず、外部サービスへ通知・データ送信する全ての機能に適用する。

- 通知本文には要約のみを送る。生ログ・ファイル内容・ユーザー入力全文は送らない。
- 送信前に必ずマスキング（`redact_structure()`相当）を経由させる。APIキー・トークン・パスワード・個人情報は対象。
- Webhook URL／トークンはパスワード相当の機密として扱い、平文で設定ファイルやログに残さない。
- 同一種別の通知にレート制限を設け、誤送信・大量送信・DoS化を防ぐ。
- Slack等のプラットフォーム提供者にも情報が渡る前提で内容を設計する（社外秘・個人情報は要約にも含めない）。
- 承認/監査/エラー通知は「何が起きたか＋参照ID」のみとし、詳細はGLM側の監査ログ参照に誘導する。

## B-4. アカウンタビリティ強化（実装方針）

- 監査ログの改ざん検知: 各レコードに直前レコードのハッシュを含めるハッシュチェーン方式を採用（実装済み: `AuditLogger`）。
- 管理者向け監査画面の二段階認証: **TOTP（Google Authenticator方式）を採用**（実装済み: `glm_security.py` TOTPヘルパー）。
- アラート・通知のSlack連携: 上記「Slack等の外部通知に関する禁止事項」に準拠して実装する。

## B-1. 攻撃ベクトル対策状況（実装済み）

| ベクトル | 対策 | 状態 |
|---|---|---|
| シンボリックリンク脱出 | `WorkspaceGuard.require_contained`がリンク自体への操作を拒否（差し替え攻撃対策） | 実装・テスト済み |
| 環境変数インジェクション | WSLサンドボックスの実行コマンドを`shlex.quote`で完全エスケープ、ファイル名を英数字等に限定 | 実装・テスト済み |
| サプライチェーン攻撃 | `pip/npm/yarn/poetry/cargo install`系を`package_install`カテゴリとして常時承認要求 | 実装・テスト済み |
| プロンプトインジェクション | `wrap_untrusted_content()`で外部由来コンテンツを明示区切り＋無効化指示付きで囲む共通ヘルパーを新設 | 実装・テスト済み（今後の外部取得機能は必ずこれを経由） |
| Zip Slip | `safe_extract_zip()`で展開先外への書き込みを事前検証・拒否 | 実装・テスト済み（現状アーカイブ展開機能は未使用のため将来機能向けの先行実装） |
| fork bomb的挙動 | WSL2実行は`ulimit -u`でプロセス数を制限済み。Windows側`command_template`実行経路（現在既定では未使用）はプロセス数制限が未実装 | 一部対応（WSL2側は対応済み、Windows直接実行経路は既知のギャップとして残置） |

新規テスト: シンボリックリンク差し替え拒否、Zip Slip拒否、シェルメタ文字ファイル名拒否、`package_install`分類、`wrap_untrusted_content`の各種を追加。全体テスト **100 passed**。

## 同時実行・障害復旧・ガバナンス評価レポート（実装済み）

- **同時実行**: `router.py`/`glm_ide_core.py`を`BoundedThreadingHTTPServer`（上限付きスレッド並列）へ変更。`SessionManager`/`BudgetTracker`にロックを追加し、実際にロックなしでは`0.5`が`0.11`まで失われる競合（lost update）を再現したうえで、ロック導入後は`0.5`と正確に一致することを実証。
- **障害復旧**: `glm_security.watch()`の判定ロジックを`evaluate_stop_reason()`として純粋関数化し、心拍断・心拍タイムアウト・STOP/LOCKファイル・親プロセス消失・最大監視時間超過・リクエスト過多・自己修復ループの全条件を個別にテストで再現・検証。
- **監査ログ改ざん検知の実運用確認**: 本番監査DB（372件）を実際に検証したところ、ハッシュチェーン導入前の318件は検証対象外（レガシー）、導入後の60件は`ok: true`で正しく検証できることを確認。導入前レコードと導入後レコードを区別する仕様に修正済み。
- **ガバナンス評価レポート**: `governance_report.py`を新設。安全性/セキュリティ/プライバシー/透明性/アカウンタビリティは実データから自動集計。**公平性・有効性は定量評価が難しいため、`--add-effectiveness`で知見を随時手動追記できるログ形式**（`~/.glm/governance_effectiveness.json`）とし、レポート生成時にそのまま反映する設計とした。


